import config from './config.json';

export const servicesData = config.services;
export const galleryData = config.gallery;
export const testimonialsData = config.testimonials;
export const faqData = config.faq;
export const instagramFeed = config.instagramFeed;
